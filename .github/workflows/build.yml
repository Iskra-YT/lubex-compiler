name: CMake Builds

on:
  push:
  pull_request:

jobs:
  build:
    name: Build on ${{ matrix.os }}
    runs-on: ${{ matrix.os }}
    strategy:
      matrix:
        os: [ubuntu-latest, windows-latest]

    steps:
      - name: Checkout repository
        uses: actions/checkout@v5

      - name: Set up CMake
        uses: lukka/get-cmake@latest

      - name: Run build script (Linux / macOS)
        if: runner.os != 'Windows'
        run: |
          chmod +x ./build.sh 
          ./build.sh

      - name: Run build script (Windows, Powershell)
        if: runner.os == 'Windows'
        shell: pwsh
        run: |
          Set-ExecutionPolicy Bypass -Scope Process -Force
          ./build.ps1

      - name: Run test script (Linux / macOS)
        if: runner.os != 'Windows'
        run: |
          chmod +x ./test.sh
          ./test.sh

      - name: Run test script (Windows, Powershell)
        if: runner.os == 'Windows'
        run: |
          Set-ExecutionPolicy Bypass -Scope Process -Force
          ./test.sh
          ./test.ps1